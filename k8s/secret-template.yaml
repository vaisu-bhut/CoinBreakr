# This is a template file. DO NOT commit actual secrets to Git!
# Create the actual secret using kubectl:
#
# kubectl create secret generic coinbreakr-secrets \
#   --from-literal=mongo-url='mongodb+srv://username:password@cluster.mongodb.net/Splitlyr?retryWrites=true&w=majority' \
#   --from-literal=jwt-secret='your-super-secret-jwt-key-here-make-it-long-and-random'
#
# Or apply this file after replacing the base64 values:
# echo -n 'your-mongo-url' | base64
# echo -n 'your-jwt-secret' | base64

apiVersion: v1
kind: Secret
metadata:
  name: coinbreakr-secrets
  labels:
    app: coinbreakr-api
    environment: testing
type: Opaque
data:
  # Base64 encoded values - REPLACE THESE WITH YOUR ACTUAL VALUES
  mongo-url: REPLACE_WITH_BASE64_ENCODED_MONGO_URL
  jwt-secret: REPLACE_WITH_BASE64_ENCODED_JWT_SECRET
