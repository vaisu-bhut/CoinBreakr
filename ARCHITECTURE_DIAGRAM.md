# Architecture Diagrams

Visual representation of the infrastructure and workflows.

## 🏗️ Testing Environment Architecture (Kubernetes)

```
┌─────────────────────────────────────────────────────────────────┐
│                         Internet                                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP/HTTPS
                             │
                    ┌────────▼────────┐
                    │  Load Balancer  │
                    │  (External IP)  │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
   ┌────▼────┐         ┌────▼────┐           ┌────▼────┐
   │  Pod 1  │         │  Pod 2  │   ...     │  Pod N  │
   │         │         │         │           │         │
   │ API:3000│         │ API:3000│           │ API:3000│
   └────┬────┘         └────┬────┘           └────┬────┘
        │                   │                     │
        └───────────────────┼─────────────────────┘
                            │
                   ┌────────▼────────┐
                   │   Kubernetes    │
                   │     Service     │
                   └────────┬────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
   ┌────▼────┐         ┌───▼────┐           ┌───▼────┐
   │ Node 1  │         │ Node 2 │   ...     │ Node N │
   │e2-medium│         │e2-medium│          │e2-medium│
   └─────────┘         └────────┘           └────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                   ┌────────▼────────┐
                   │   GKE Cluster   │
                   │  (us-central1)  │
                   └─────────────────┘
```

## 🔄 CI/CD Workflow - Testing Branch

```
┌──────────────────────────────────────────────────────────────────┐
│                    Developer Workflow                            │
└──────────────────────────────────────────────────────────────────┘

    Developer
        │
        │ git push origin testing
        │
        ▼
┌───────────────────┐
│  GitHub Actions   │
│   (PR Workflow)   │
└────────┬──────────┘
         │
         ├─► Lint Code
         ├─► Security Audit
         ├─► Docker Build Test
         ├─► Security Scan (Trivy)
         ├─► K8s Validation
         └─► Terraform Validate
         
    Merge to testing
        │
        ▼
┌───────────────────┐
│  GitHub Actions   │
│ (Deploy Workflow) │
└────────┬──────────┘
         │
         ├─► 1. Build Docker Image
         │      └─► services/Dockerfile
         │
         ├─► 2. Security Scan
         │      └─► Trivy vulnerability scan
         │
         ├─► 3. Push to Registry
         │      └─► us-central1-docker.pkg.dev/
         │          coinbreakr/coinbreakr-testing/
         │          services:commit-sha
         │
         ├─► 4. Deploy to GKE
         │      ├─► Apply ConfigMap
         │      ├─► Apply Deployment
         │      ├─► Apply Service
         │      └─► Apply HPA
         │
         ├─► 5. Update Image
         │      └─► kubectl set image
         │
         ├─► 6. Wait for Rollout
         │      └─► kubectl rollout status
         │
         └─► 7. Health Check
                └─► curl /v1/healthz
```

## 🔀 Branch Comparison

```
┌─────────────────────────────────────────────────────────────────┐
│                         Main Branch                              │
│                       (Production)                               │
└─────────────────────────────────────────────────────────────────┘

    Push to main
        │
        ▼
    Packer Build
        │
        ├─► Create VM Image
        │   └─► Install Node.js
        │   └─► Deploy Code
        │   └─► Configure systemd
        │
        ▼
    Store in GCP
        │
        └─► Compute Engine Images
            └─► coinbreakr-production-family

    Manual Terraform Apply
        │
        ▼
    Deploy VMs
        │
        ├─► Instance Template
        ├─► Managed Instance Group (2-10 VMs)
        ├─► Load Balancer
        └─► DNS (api.splitlyr.clestiq.com)

┌─────────────────────────────────────────────────────────────────┐
│                       Testing Branch                             │
│                    (Kubernetes - NEW)                            │
└─────────────────────────────────────────────────────────────────┘

    Push to testing
        │
        ▼
    Docker Build
        │
        ├─► Build Container Image
        │   └─► services/Dockerfile
        │
        ▼
    Push to Registry
        │
        └─► Artifact Registry
            └─► coinbreakr-testing/services

    Automatic Deployment
        │
        ▼
    Deploy to GKE
        │
        ├─► Kubernetes Deployment (2-10 pods)
        ├─► Service (LoadBalancer)
        ├─► HPA (Auto-scaling)
        └─► External IP (no DNS)
```

## 📦 Kubernetes Resource Hierarchy

```
┌─────────────────────────────────────────────────────────────────┐
│                       GKE Cluster                                │
│                coinbreakr-testing-cluster                        │
└─────────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
   ┌────▼────┐         ┌───▼────┐         ┌───▼────┐
   │ Node 1  │         │ Node 2 │         │ Node 3 │
   │         │         │        │         │        │
   │ ┌─────┐ │         │ ┌────┐ │         │ ┌────┐ │
   │ │Pod 1│ │         │ │Pod2│ │         │ │Pod3│ │
   │ └─────┘ │         │ └────┘ │         │ └────┘ │
   │ ┌─────┐ │         │ ┌────┐ │         │        │
   │ │Pod 4│ │         │ │Pod5│ │         │        │
   │ └─────┘ │         │ └────┘ │         │        │
   └─────────┘         └────────┘         └────────┘

Each Pod contains:
┌──────────────────────────────────────┐
│           Pod                        │
│  ┌────────────────────────────────┐ │
│  │  Container: api                │ │
│  │  Image: services:commit-sha    │ │
│  │  Port: 3000                    │ │
│  │                                │ │
│  │  Resources:                    │ │
│  │    Request: 250m CPU, 256Mi    │ │
│  │    Limit: 500m CPU, 512Mi      │ │
│  │                                │ │
│  │  Env from:                     │ │
│  │    - coinbreakr-secrets        │ │
│  │    - coinbreakr-config         │ │
│  │                                │ │
│  │  Health Checks:                │ │
│  │    - Liveness: /v1/healthz     │ │
│  │    - Readiness: /v1/healthz    │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
```

## 🔄 Auto-scaling Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    Auto-scaling Triggers                         │
└─────────────────────────────────────────────────────────────────┘

    High Load
        │
        ├─► CPU > 70%  ──┐
        │                │
        └─► Memory > 80% ┘
                │
                ▼
        ┌───────────────┐
        │      HPA      │
        │  (Horizontal  │
        │     Pod       │
        │  Autoscaler)  │
        └───────┬───────┘
                │
                ├─► Scale Up Pods (2 → 10)
                │   └─► Add new pods to deployment
                │
                ▼
        More pods needed?
                │
                ├─► Yes: Node pool scales up (1 → 5)
                │        └─► GKE adds new nodes
                │
                └─► No: Use existing nodes

    Low Load
        │
        ├─► CPU < 70%  ──┐
        │                │
        └─► Memory < 80% ┘
                │
                ▼
        ┌───────────────┐
        │      HPA      │
        └───────┬───────┘
                │
                ├─► Scale Down Pods (10 → 2)
                │   └─► Remove pods (5 min stabilization)
                │
                ▼
        Nodes underutilized?
                │
                └─► Yes: Node pool scales down (5 → 1)
                         └─► GKE removes nodes
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      Security Layers                             │
└─────────────────────────────────────────────────────────────────┘

    Internet
        │
        ▼
    ┌─────────────────┐
    │ Load Balancer   │  ◄─── Layer 1: Network Entry Point
    │ (External IP)   │       - Public endpoint
    └────────┬────────┘       - DDoS protection
             │
             ▼
    ┌─────────────────┐
    │ Kubernetes      │  ◄─── Layer 2: Service Mesh
    │ Service         │       - Internal routing
    └────────┬────────┘       - Service discovery
             │
             ▼
    ┌─────────────────┐
    │ Pods            │  ◄─── Layer 3: Container Isolation
    │ (Containers)    │       - Resource limits
    └────────┬────────┘       - Network policies
             │
             ▼
    ┌─────────────────┐
    │ Application     │  ◄─── Layer 4: Application Security
    │ (Node.js API)   │       - JWT authentication
    └────────┬────────┘       - Input validation
             │                 - Rate limiting
             ▼
    ┌─────────────────┐
    │ Secrets         │  ◄─── Layer 5: Data Protection
    │ (K8s Secrets)   │       - Encrypted at rest
    └────────┬────────┘       - RBAC access
             │
             ▼
    ┌─────────────────┐
    │ MongoDB         │  ◄─── Layer 6: Database Security
    │ (External)      │       - Connection encryption
    └─────────────────┘       - Authentication required
```

## 📊 Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    API Request Flow                              │
└─────────────────────────────────────────────────────────────────┘

    Mobile App
        │
        │ POST /v1/auth/login
        │ { email, password }
        │
        ▼
    Load Balancer (External IP)
        │
        │ Route to healthy pod
        │
        ▼
    Kubernetes Service
        │
        │ Select pod based on:
        │ - Health status
        │ - Current load
        │
        ▼
    Pod (Container)
        │
        ├─► 1. Receive request (Express)
        │
        ├─► 2. Middleware
        │      ├─► CORS
        │      ├─► Body parser
        │      └─► Security headers
        │
        ├─► 3. Route handler
        │      └─► /v1/auth/login
        │
        ├─► 4. Controller
        │      ├─► Validate input
        │      ├─► Query MongoDB
        │      ├─► Verify password
        │      └─► Generate JWT
        │
        ├─► 5. Response
        │      └─► { success, token, user }
        │
        ▼
    Return to Mobile App
```

## 🗄️ Storage Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Storage Components                            │
└─────────────────────────────────────────────────────────────────┘

    ┌──────────────────────┐
    │  Artifact Registry   │  ◄─── Docker Images
    │  (us-central1)       │       - services:commit-sha
    │                      │       - services:latest
    │  Cleanup Policy:     │       - Retention: 30 days
    │  - Keep 10 versions  │       - Keep 10 versions
    │  - Delete after 30d  │
    └──────────────────────┘

    ┌──────────────────────┐
    │  Persistent Disks    │  ◄─── Node Storage
    │  (per node)          │       - 50GB per node
    │                      │       - pd-standard
    │  Contains:           │       - Container images
    │  - Container images  │       - Logs
    │  - Logs              │
    └──────────────────────┘

    ┌──────────────────────┐
    │  Kubernetes Secrets  │  ◄─── Sensitive Data
    │  (etcd)              │       - mongo-url
    │                      │       - jwt-secret
    │  Encrypted at rest   │       - Encrypted
    └──────────────────────┘

    ┌──────────────────────┐
    │  MongoDB Atlas       │  ◄─── Application Data
    │  (External)          │       - Users
    │                      │       - Groups
    │  Collections:        │       - Expenses
    │  - users             │       - Friends
    │  - groups            │
    │  - expenses          │
    │  - pendingfriends    │
    └──────────────────────┘
```

## 🔄 Deployment Process

```
┌─────────────────────────────────────────────────────────────────┐
│                  Rolling Update Process                          │
└─────────────────────────────────────────────────────────────────┘

    Initial State (v1.0.0)
    ┌────┐ ┌────┐ ┌────┐ ┌────┐
    │ v1 │ │ v1 │ │ v1 │ │ v1 │  ◄─── 4 pods running
    └────┘ └────┘ └────┘ └────┘

    Deploy v1.0.1
        │
        ▼
    Step 1: Create new pod
    ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐
    │ v1 │ │ v1 │ │ v1 │ │ v1 │ │ v2 │  ◄─── New pod starting
    └────┘ └────┘ └────┘ └────┘ └────┘
                                   │
                                   ▼
                              Wait for ready
        │
        ▼
    Step 2: Terminate old pod
    ┌────┐ ┌────┐ ┌────┐ ┌────┐
    │ v1 │ │ v1 │ │ v1 │ │ v2 │  ◄─── Old pod terminated
    └────┘ └────┘ └────┘ └────┘
        │
        ▼
    Step 3: Repeat
    ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐
    │ v1 │ │ v1 │ │ v1 │ │ v2 │ │ v2 │
    └────┘ └────┘ └────┘ └────┘ └────┘
        │
        ▼
    Final State (v1.0.1)
    ┌────┐ ┌────┐ ┌────┐ ┌────┐
    │ v2 │ │ v2 │ │ v2 │ │ v2 │  ◄─── All pods updated
    └────┘ └────┘ └────┘ └────┘

    Zero Downtime! ✅
```

---

**Note**: These are simplified diagrams. Actual implementation includes additional components like health checks, monitoring, logging, and security features.
