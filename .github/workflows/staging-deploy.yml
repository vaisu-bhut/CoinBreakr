name: Deploy to Staging

on:
  workflow_run:
    workflows: ["Build Custom Image with Packer"]
    types: [completed]
    branches: [staging]

permissions:
  id-token: write
  contents: read

jobs:
  deploy-staging:
    name: Deploy to Staging Environment
    if: github.repository == 'Clestique/CoinBreakr' && ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout Repository
        uses: actions/checkout@v4

      - name: ☁️ Authenticate to Google Cloud (Workload Identity)
        uses: google-github-actions/auth@v1
        with:
          workload_identity_provider: projects/379416821819/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service_account: packer-github@coinbreakr.iam.gserviceaccount.com

      - name: 🛠️ Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: coinbreakr

      - name: 🔍 Verify Authentication
        run: gcloud auth list

      - name: 📦 Get Latest Image
        id: get-image
        run: |
          echo "🔍 Finding latest image from coinbreakr-image-family..."
          LATEST_IMAGE=$(gcloud compute images list --filter="family:coinbreakr-staging-family" --format="value(name)" --sort-by="~creationTimestamp" --limit=1)
          if [ -z "$LATEST_IMAGE" ]; then
            echo "❌ No images found in coinbreakr-image-family"
            exit 1
          fi
          echo "✅ Found latest image: $LATEST_IMAGE"
          echo "image_name=$LATEST_IMAGE" >> $GITHUB_OUTPUT
          echo "image_url=gcr.io/coinbreakr/$LATEST_IMAGE" >> $GITHUB_OUTPUT

      - name: 🏗️ Setup Terraform
        run: |
          echo "📦 Installing Terraform..."
          wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
          sudo apt update && sudo apt install terraform -y
          terraform --version

      - name: 🗂️ Initialize Terraform
        working-directory: ./terraform
        run: |
          echo "🗂️ Initializing Terraform..."
          terraform init

      - name: ✅ Validate Terraform Configuration
        working-directory: ./terraform
        run: |
          echo "✅ Validating Terraform configuration..."
          terraform validate

      - name: 🚀 Apply Terraform Deployment
        working-directory: ./terraform
        run: |
          echo "🚀 Applying Terraform deployment..."
          COMMIT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)  # Short SHA
          INSTANCE_NAME="coinbreakr-$COMMIT_SHA"

          terraform apply -auto-approve -var="project_id=${{ vars.GCP_PROJECT_ID }}" -var="zone=${{ vars.ZONE }}" -var="instance_name=$INSTANCE_NAME" -var="image_family=coinbreakr-staging-family" -var="machine_type=${{ vars.MACHINE_TYPE_TERRAFORM }}" -var="ssh_user=${{ vars.USERNAME_TERRAFORM }}" -var="ssh_public_key_path=/dev/null"

      - name: 📊 Get Instance Information
        working-directory: ./terraform
        run: |
          echo "📊 Getting instance information..."
          INSTANCE_NAME=$(terraform output -raw instance_name 2>/dev/null)
          EXTERNAL_IP=$(terraform output -raw instance_external_ip 2>/dev/null)
          
          echo "🎉 Deployment completed!"
          echo "Instance Name: $INSTANCE_NAME"
          echo "External IP: $EXTERNAL_IP"
          echo "Application URL: http://$EXTERNAL_IP:3000/v1/healthz"
