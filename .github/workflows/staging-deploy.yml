name: Deploy to Staging

on:
  workflow_run:
    workflows: ["Build Custom Image with Packer"]
    types: [completed]
    branches: [staging]

permissions:
  id-token: write
  contents: read

jobs:
  deploy-staging:
    name: Deploy to Staging Environment
    if: github.repository == 'Clestique/CoinBreakr' && ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout Repository
        uses: actions/checkout@v4

      - name: ☁️ Authenticate to Google Cloud (Workload Identity)
        uses: google-github-actions/auth@v1
        with:
          workload_identity_provider: projects/379416821819/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service_account: packer-github@coinbreakr.iam.gserviceaccount.com

      - name: 🛠️ Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: coinbreakr

      - name: 🔍 Verify Authentication
        run: gcloud auth list

      - name: 📦 Get Latest Image
        id: get-image
        run: |
          echo "🔍 Finding latest image from coinbreakr-image-family..."
          LATEST_IMAGE=$(gcloud compute images list --filter="family:coinbreakr-image-family" --format="value(name)" --sort-by="~creationTimestamp" --limit=1)
          if [ -z "$LATEST_IMAGE" ]; then
            echo "❌ No images found in coinbreakr-image-family"
            exit 1
          fi
          echo "✅ Found latest image: $LATEST_IMAGE"
          echo "image_name=$LATEST_IMAGE" >> $GITHUB_OUTPUT
          echo "image_url=gcr.io/coinbreakr/$LATEST_IMAGE" >> $GITHUB_OUTPUT

      - name: 🏗️ Setup Terraform
        run: |
          echo "📦 Installing Terraform..."
          wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
          sudo apt update && sudo apt install terraform -y
          terraform --version

      - name: 🗂️ Initialize Terraform
        working-directory: ./terraform
        run: |
          echo "🗂️ Initializing Terraform..."
          terraform init

      - name: ✅ Validate Terraform Configuration
        working-directory: ./terraform
        run: |
          echo "✅ Validating Terraform configuration..."
          terraform validate

      - name: 📋 Plan Terraform Deployment
        working-directory: ./terraform
        run: |
          echo "📋 Planning Terraform deployment..."
          terraform plan \
            -var="project_id=coinbreakr" \
            -var="region=us-central1" \
            -var="zone=us-central1-a" \
            -var="instance_name=coinbreakr-staging-$(date +%Y%m%d-%H%M%S)" \
            -var="image_family=coinbreakr-image-family" \
            -var="machine_type=e2-small" \
            -var="ssh_user=terraform" \
            -var="ssh_public_key_path=/dev/null" \
            -out=tfplan

      - name: 🚀 Apply Terraform Deployment
        working-directory: ./terraform
        run: |
          echo "🚀 Applying Terraform deployment..."
          terraform apply -auto-approve \
            -var="project_id=coinbreakr" \
            -var="region=us-central1" \
            -var="zone=us-central1-a" \
            -var="instance_name=coinbreakr-staging-$(date +%Y%m%d-%H%M%S)" \
            -var="image_family=coinbreakr-image-family" \
            -var="machine_type=e2-small" \
            -var="ssh_user=terraform" \
            -var="ssh_public_key_path=/dev/null"

      - name: 📊 Get Instance Information
        working-directory: ./terraform
        run: |
          echo "📊 Getting instance information..."
          INSTANCE_NAME=$(terraform output -raw instance_name 2>/dev/null || echo "coinbreakr-staging-$(date +%Y%m%d-%H%M%S)")
          EXTERNAL_IP=$(gcloud compute instances describe $INSTANCE_NAME --zone=us-central1-a --format="value(networkInterfaces[0].accessConfigs[0].natIP)" 2>/dev/null || echo "Not available")
          
          echo "🎉 Deployment completed!"
          echo "Instance Name: $INSTANCE_NAME"
          echo "External IP: $EXTERNAL_IP"
          echo "Application URL: http://$EXTERNAL_IP:3000"
          
          # Set outputs for other steps
          echo "instance_name=$INSTANCE_NAME" >> $GITHUB_OUTPUT
          echo "external_ip=$EXTERNAL_IP" >> $GITHUB_OUTPUT

      - name: 🔍 Verify Deployment
        run: |
          echo "🔍 Verifying deployment..."
          if [ -n "${{ steps.get-instance.outputs.external_ip }}" ] && [ "${{ steps.get-instance.outputs.external_ip }}" != "Not available" ]; then
            echo "✅ Instance is running with IP: ${{ steps.get-instance.outputs.external_ip }}"
            echo "🌐 Application should be available at: http://${{ steps.get-instance.outputs.external_ip }}:3000"
          else
            echo "⚠️ Could not retrieve external IP, but deployment completed"
          fi

      - name: 📝 Create Deployment Summary
        run: |
          echo "## 🚀 Staging Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ✅ Successfully deployed to staging" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Instance Details:**" >> $GITHUB_STEP_SUMMARY
          echo "- **Instance Name:** ${{ steps.get-instance.outputs.instance_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **External IP:** ${{ steps.get-instance.outputs.external_ip }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Application URL:** http://${{ steps.get-instance.outputs.external_ip }}:3000" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Next Steps:**" >> $GITHUB_STEP_SUMMARY
          echo "1. Test the application at the provided URL" >> $GITHUB_STEP_SUMMARY
          echo "2. Monitor logs and performance" >> $GITHUB_STEP_SUMMARY
          echo "3. Promote to production if tests pass" >> $GITHUB_STEP_SUMMARY
