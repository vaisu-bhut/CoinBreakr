**CoinBreakr Server**

**Tasks:-**

1. `main`, `staging` and `dev` branch on Org's Repository
2. Idempotent & Isolate automatic terraform deployment on `main` & `staging` branch only
3. Test Suites
4. Securing Secrets on Deployment Scripts
5. Differentiate Storage Solutions
6. Load Balancing & Auto-Scaling